[workspace]
members = [
    "crates/scss_rust",
    "crates/include_sass",
    "crates/lib",
]
resolver = "2"

[profile.release]
debug = 1
panic = "abort"
lto = true
codegen-units = 1

[profile.small]
inherits = 'release'
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true

